import { NoSecialCharDirective } from './no-secial-char.directive';

describe('NoSecialCharDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSecialCharDirective();
    expect(directive).toBeTruthy();
  });
});
